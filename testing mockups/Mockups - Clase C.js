// Clase lógica (C) — VACÍA
// Esta pantalla es template-only: no define estado ni renderVals().
// Toda la composición vive en el HTML estructural (inline styles).

class Component extends DCLogic {
  renderVals() {
    return {};
  }
}
